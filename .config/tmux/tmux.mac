# set bar on top
set-option -g status-position top


# Use emacs key bindings (no vi mode)
setw -g mode-keys emacs

# rebind prefix to ctrl-
set -g prefix C-s

# Bitwarden
source-file ~/.config/tmux/secrets.conf
