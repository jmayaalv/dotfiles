# /Users/jmaya/.config/tmuxinator/sbi_test.yml

name: sbi-test
root: ~/
 # Controls whether the tmux session should be attached to automatically. Defaults to true.
# attach: false

windows:
  - test1: ssh -C -o 'ServerAliveInterval 180' devel.jmayaalv@sbitest1
  - test2: ssh -C -o 'ServerAliveInterval 180' devel.jmayaalv@sbitest2
  - test3: ssh -C -o 'ServerAliveInterval 180' devel.jmayaalv@sbitest3
  - test4: ssh -C -o 'ServerAliveInterval 180' devel.jmayaalv@sbitest4
