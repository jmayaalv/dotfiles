# /Users/jmaya/.config/tmuxinator/glacier_test.yml

name: glacier-test
root: ~/

# Controls whether the tmux session should be attached to automatically. Defaults to true.
# attach: false

windows:
  - Test1: ssh -C -o 'ServerAliveInterval 180' devel.jmayaalv@glaciertest1
  - Test2: ssh -C -o 'ServerAliveInterval 180' devel.jmayaalv@glaciertest2
  - Test3: ssh -C -o 'ServerAliveInterval 180' devel.jmayaalv@glaciertest3
  - Test4: ssh -C -o 'ServerAliveInterval 180' devel.jmayaalv@glaciertest4
